<%= form_for(@equipo) do |f| %>
  <% if @equipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipo.errors.count, "error") %> prohibited this equipo from being saved:</h2>

      <ul>
      <% @equipo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
    <%= f.label :grupo %><br />
    <%= f.collection_select :grupo_id, Grupo.all, :id, :nombre %>
  </div>
  
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :fundacion %><br />
    <%= f.date_select :fundacion %>
  </div>
  <div class="field">
    <%= f.label :historia %><br />
    <%= f.text_area :historia %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
